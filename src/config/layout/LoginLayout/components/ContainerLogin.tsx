import { Grid2, styled } from '@mui/material';

const ContainerLogin = styled(Grid2)({
    height: '100vh',
    justifyContent: 'center',
    alignItems: 'center',
    background: 'url("/images/background.jpg") center/ cover no-repeat'
});

export default ContainerLogin;